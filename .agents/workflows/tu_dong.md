---
description: Quy trình phát triển tự động cho website Việt Thành
---

// turbo-all

1. Chạy server phát triển:
   `npm run dev` trong thư mục `C:\Users\Dell OPT 3020\.gemini\antigravity\scratch\vithacon-react`

2. Kiểm tra trạng thái dự án:
   `git status` (nếu có) hoặc kiểm tra các file trong `src/components`

3. Cập nhật nội dung:
   Sử dụng các công cụ chỉnh sửa file để cập nhật thông tin từ vithacon.vn
